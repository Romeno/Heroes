using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Pathfinding;

public class Heroes : MonoBehaviour
{
    Battlefield bf;

    void Start()
    {
        

        //((GridGraph)AstarPath.active.graphs[0]).SetDimensions( bf.GetCellSize());
        //AstarPath.active.Scan();
    }

    void Update()
    {
        
    }
}
